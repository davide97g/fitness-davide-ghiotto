import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'com.ghiotto.davidenko',
	appName: 'fitness-davide-ghiotto',
	webDir: 'dist',
	server: {
		androidScheme: 'https',
	},
};

export default config;
